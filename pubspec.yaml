name: local_ref
description: "A simple, powerful state management library for Flutter applications. Inspired by Vue3's ref."
version: 0.0.1
homepage: https://github.com/Aykahshi/local_ref
repository: https://github.com/Aykahshi/local_ref

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0